/*

bubble sort 

1. compare 2 numbers 
2. swap if necessary 
3. repeat until all done 


for i = 0 to i< N-1 do

    for j = 0 to j < N-1 do

        if A[j] > A[j+1]

            temp = A[j]
            A[j] = A[j+1]
            A[j+1]= temp

        end if 

    end for inner 
    
end for outer 


*/
-----------------------------------
/*

loop 
    loop 
        if 
        end if 
    end loop 
end loop 

---------------------------

loop 
    loop 
        if A[j]>A[j+1]

            // use temp instead of "swap"
            swap (A[i], A[j]) 

        end if 
    end loop 
end loop 

-----------------------

loop (i = 0; i<N-1; i++)

    loop (j=0; j<N; j++)

        if A[j]>A[j+1]
        
            // use temp instead of "swap"
            swap (A[i], A[j]) 

        end if 
    end loop 
end loop 
--------------------------

loop (i = 0; i<N-1; i++)

    loop (j=0; j<N; j++)

        if A[j]>A[j+1]
        
            // use temp instead of "swap"
            swap (A[i], A[j]) 

        end if 
    end loop 
end loop 

--------------------------






*/